class Computer
	attr_reader :marker

	def initialize(marker)
		@marker = marker
	end

	def cpu_move(board)
        
        value = Human.get_move(move)
    end
 
end
# 		cpu = Human.move + 1

# 			if cpu >= 8
# 				cpu == 0
# 			end
# 			while board[cpu] == board.marker
# 				cpu = Human.move + 1
# 			end

# 		if board[cpu] == ""
# 			cpu
# 		end
# 	end
		
# end